import { Accordion } from "@mui/material";
import { styled } from "@mui/system";

const ListDropdownItem = styled(Accordion)({
  backgroundColor: "#848484",
  width: "50%",
});

export default ListDropdownItem;
